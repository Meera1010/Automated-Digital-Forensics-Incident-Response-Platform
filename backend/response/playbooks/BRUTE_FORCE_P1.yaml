name: "Brute Force Response - Critical (P1)"
attack_category: BRUTE_FORCE
severity_tier: P1
actions:
  - action: block_ip
    params:
      ip_address: "192.168.1.100"
      duration_minutes: 1440
      reason: "P1 brute force — extended block"
  - action: disable_account
    params:
      username: "targeted_user"
      reason: "P1 — account disabled pending investigation"
  - action: contain_incident
    params:
      reason: "P1 brute force — incident contained"
